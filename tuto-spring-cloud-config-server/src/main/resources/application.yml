spring:  
  devtools:
    add-properties: false
  application:
    name: server-config
  cloud:
    config:
      server:
        bootstrap: true
        git:
          uri: ${user.home}/workspace/0-projects/tuto-config-repos
          clone-on-start:  true

server:
  port: 8181
